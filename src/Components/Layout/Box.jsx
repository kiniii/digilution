import styled from "styled-components";
import { layout, space, grid, flexbox, display } from "styled-system";

const Box = styled.div`
    ${display}
    ${layout}
    ${space}
    ${grid}
    ${flexbox}
`

export default Box;